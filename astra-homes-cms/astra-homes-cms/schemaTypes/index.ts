import property from './property';
import interiorProject from './interiorProject';
import constructionProject from './constructionProject';
import about from './about';

export const schemaTypes = [property, interiorProject, constructionProject, about];
